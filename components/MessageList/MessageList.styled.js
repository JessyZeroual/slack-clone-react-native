import styled from 'styled-components';
import { Container as ContainerCommon } from '../../styled.common';

// eslint-disable-next-line import/prefer-default-export
export const Container = styled(ContainerCommon)`
  flex: 1;
`;
